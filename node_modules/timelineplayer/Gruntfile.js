module.exports = function(grunt) {
  grunt.loadNpmTasks('grunt-contrib-uglify');


  grunt.initConfig({
    uglify: {
      all: {
        files: { 'timelineplayer.min.js': 'timelineplayer.js' },
        options: { banner: '// TimelinePlayer.js - https://github.com/thunder9/timelineplayer.js\n' }
      }
    }
  });


  grunt.registerTask('default', ['uglify']);
};